{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'COMMAND=$(jq -r .tool_input.command); if echo \"$COMMAND\" | grep -qE \"^\\s*git\\s+commit\"; then typos --write-changes 2>&1 || true; fi'",
            "statusMessage": "Running typos --write-changes"
          }
        ]
      }
    ]
  }
}
